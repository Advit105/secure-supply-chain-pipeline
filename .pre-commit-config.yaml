# Install once:  pip install pre-commit && pre-commit install
# Runs on every `git commit`; blocks the commit if a secret is found.
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.4
    hooks:
      - id: gitleaks
