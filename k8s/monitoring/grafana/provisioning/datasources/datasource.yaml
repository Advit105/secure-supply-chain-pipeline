apiVersion: 1
datasources:
  - name: Prometheus
    uid: prometheus # referenced by uid in the provisioned dashboard panels
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
